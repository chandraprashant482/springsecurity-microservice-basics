server:
  port: 8000

spring:
  application:
    name: authService

  datasource:
    url: jdbc:mysql://localhost:3306/authdb
    username: root
    password: 231199
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true